export default function AssignmentCard({ assignment, submitted, onConfirm }) {
  const due = new Date(assignment.dueDate);
  const overdue = due < new Date() && !submitted;
  return <article className="flex h-full flex-col rounded-lg border border-slate-200 bg-white p-5 shadow-sm transition hover:border-emerald-200 hover:shadow-md"><div className="mb-4 flex items-center justify-between gap-4"><span className={`rounded-full px-3 py-1 text-xs font-semibold ${overdue ? 'bg-red-50 text-red-700' : submitted ? 'bg-emerald-50 text-emerald-700' : 'bg-sky-50 text-sky-700'}`}>{submitted ? 'Submitted' : overdue ? 'Overdue' : 'Open'}</span><span className="text-xs font-medium text-slate-500">Due {due.toLocaleDateString()}</span></div><h3 className="mb-2 text-lg font-semibold text-slate-900">{assignment.title}</h3><p className="flex-1 text-sm leading-6 text-slate-600">{assignment.description || 'No description provided.'}</p><div className="mt-5 flex flex-wrap items-center justify-between gap-3 border-t border-slate-100 pt-4">{assignment.onedriveLink && <a className="text-sm font-semibold text-emerald-800 hover:text-emerald-950" href={assignment.onedriveLink} target="_blank" rel="noreferrer">Open brief ↗</a>}{onConfirm && !submitted && <button className="rounded bg-emerald-900 px-4 py-2 text-sm font-semibold text-white transition hover:bg-emerald-800" onClick={() => onConfirm(assignment.id)}>Confirm submission</button>}</div></article>;
}